// Setup file for vitest
// This replaces the jest-chain setup in Jest config

// Vitest has built-in support for chained matchers, so no additional setup is needed
// for the functionality that jest-chain provided.
